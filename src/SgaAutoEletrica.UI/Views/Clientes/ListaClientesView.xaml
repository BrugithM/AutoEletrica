<UserControl x:Class="SgaAutoEletrica.UI.Views.Clientes.ListaClientesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.Row="0"
                Text="Clientes"
                FontSize="24"
                FontWeight="Bold"
                Margin="0,0,0,15"/>

        <!-- Barra de busca -->
        <Grid Grid.Row="1"
                Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding TermoBusca, UpdateSourceTrigger=PropertyChanged}"
                     Height="30"
                    FontSize="14"
                    Padding="5"
                    Margin="0,0,10,0"
                     ToolTip="Busca por nome"/>

            <Button Grid.Column="1"
                    Content="Buscar"
                    Command="{Binding BuscarCommand}"
                    Width="80"
                    Height="30"
                    Margin="0,0,10,0"/>

            <Button Grid.Column="2"
                    Content="Limpar"
                    Command="{Binding LimparBuscaCommand}"
                    Width="80"
                    Height="30"
                    Margin="0,0,10,0"/>

            <Button Grid.Column="3"
                    Content="+ Novo Cliente"
                    Command="{Binding NovoClienteCommand}"
                    Width="120"
                    Height="30"
                    Background="#27AE60"
                    Foreground="White"/>
        </Grid>

        <!-- Lista de clientes -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Clientes}"
                  SelectedItem="{Binding ClienteSelecionado}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome Completo"
                        Binding="{Binding NomeCompleto}"
                        Width="300"/>
                <DataGridTextColumn Header="CPF"
                        Binding="{Binding Cpf}"
                        Width="150"/>
                <DataGridTextColumn Header="Telefone"
                        Binding="{Binding Telefone}"
                        Width="130"/>
                <DataGridTextColumn Header="Veículos"
                        Binding="{Binding QuantidadeVeiculos}"
                        Width="80"/>
                <DataGridTextColumn Header="Cadastro"
                        Binding="{Binding DataCadastro, StringFormat=dd/MM/yyyy}"
                        Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Botões de ação sobre o item selecionado -->
        <StackPanel Grid.Row="3"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,10,0,0">
            <Button Content="Editar"
                    Command="{Binding EditarClienteCommand}"
                    Width="80"
                    Height="30"
                    Margin="0,0,10,0"/>
            <Button Content="Excluir"
                    Command="{Binding ExcluirClienteCommand}"
                    Width="80"
                    Height="30"
                    Background="#C0392B"
                    Foreground="White"
                    Visibility="{Binding EhAdministrador, Converter={StaticResource BoolParaVisibilidade}}"/>
        </StackPanel>
    </Grid>
</UserControl>