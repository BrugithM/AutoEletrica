<UserControl x:Class="SgaAutoEletrica.UI.Views.Configuracoes.BackupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Backup do Sistema" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Realizar Backup Agora" Command="{Binding RealizarBackupCommand}"
                    Width="170" Height="35" Background="#27AE60" Foreground="White" FontWeight="SemiBold"
                    Margin="0,0,10,0"/>
            <Button Content="Restaurar Backup" Command="{Binding RestaurarCommand}"
                    Width="140" Height="35" Background="#E67E22" Foreground="White" FontWeight="SemiBold"
                    Margin="0,0,10,0"/>
            <Button Content="Atualizar Lista" Command="{Binding AtualizarCommand}"
                    Width="130" Height="35"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Backups}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Arquivo de Backup" Binding="{Binding}" Width="350"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>