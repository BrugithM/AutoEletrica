<Window x:Class="SgaAutoEletrica.UI.Views.Servicos.CadastroServicoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Titulo}" Height="360" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ficha Cadastral:" FontWeight="Bold" FontSize="13" Margin="0,0,0,10"/>

        <GroupBox Grid.Row="1" Header="Serviço" Padding="10">
            <StackPanel>
                <TextBlock Text="Nome"/>
                <TextBox Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" 
                         Height="26" Margin="0,2,0,10"/>

                <TextBlock Text="Descrição"/>
                <TextBox Text="{Binding Descricao, UpdateSourceTrigger=PropertyChanged}" 
                         Height="50" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,2,0,10"/>

                <TextBlock Text="Preço"/>
                <TextBox Text="{Binding PrecoPadrao, UpdateSourceTrigger=LostFocus, Converter={StaticResource DecimalConverter}}" 
                         Height="26" Margin="0,2,0,0"/>
            </StackPanel>
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Salvar" Width="90" Height="30" Margin="0,0,10,0" Click="BtnSalvar_Click"/>
            <Button Content="Limpar" Width="90" Height="30" Click="BtnLimpar_Click"/>
        </StackPanel>
    </Grid>
</Window>