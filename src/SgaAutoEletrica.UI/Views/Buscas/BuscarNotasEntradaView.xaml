<UserControl x:Class="SgaAutoEletrica.UI.Views.Buscas.BuscarNotasEntradaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Buscar Notas de Entrada" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="5" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <TextBlock Text="Nome do Fornecedor:" FontWeight="SemiBold" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NomeFornecedor, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,5,0">
                        <TextBlock Text="CNPJ do Fornecedor:" FontWeight="SemiBold" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding CnpjFornecedor, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Código do Produto:" FontWeight="SemiBold" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding CodigoProduto, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
                    </StackPanel>
                </Grid>

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <TextBlock Text="Nome do Produto:" FontWeight="SemiBold" Margin="0,0,0,3"/>
                        <TextBox Text="{Binding NomeProduto, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,5,0">
                        <TextBlock Text="Data Início:" FontWeight="SemiBold" Margin="0,0,0,3"/>
                        <DatePicker SelectedDate="{Binding DataInicio}" Height="30"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Data Fim:" FontWeight="SemiBold" Margin="0,0,0,3"/>
                        <DatePicker SelectedDate="{Binding DataFim}" Height="30"/>
                    </StackPanel>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <TextBlock Text="Busca parcial e não diferencia maiúsculas/minúsculas"
                               FontSize="11" Foreground="#666" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Content="Limpar" Command="{Binding LimparCommand}" Width="80" Height="30" Margin="0,0,10,0"/>
                    <Button Content="Buscar" Command="{Binding BuscarCommand}" Width="100" Height="30" 
                            Background="#3498DB" Foreground="White" FontWeight="SemiBold"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Resultados}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Número" Binding="{Binding Numero}" Width="100"/>
                <DataGridTextColumn Header="Data Entrada" Binding="{Binding DataEntrada, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="Fornecedor" Binding="{Binding NomeFornecedor}" Width="200"/>
                <DataGridTextColumn Header="CNPJ" Binding="{Binding CnpjFornecedor}" Width="150"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding ValorTotal, StringFormat=C2}" Width="90"/>
                <DataGridTextColumn Header="Observação" Binding="{Binding Observacao}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>