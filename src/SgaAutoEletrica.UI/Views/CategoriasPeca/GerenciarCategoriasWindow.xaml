<Window x:Class="SgaAutoEletrica.UI.Views.CategoriasPeca.GerenciarCategoriasWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gerenciar Categorias"
        Width="400"
        SizeToContent="Height"
        MaxHeight="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                Text="Categorias Cadastradas:"
                FontWeight="Bold"
                FontSize="14"
                Margin="0,0,0,10"/>

        <ScrollViewer Grid.Row="1"
                MaxHeight="400"
                VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Categorias}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#F5F5F5"
                                CornerRadius="4"
                                Padding="10,8"
                                Margin="0,0,0,5">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0"
                                        Orientation="Horizontal">
                                    <TextBlock Text="{Binding Nome}"
                                            FontWeight="SemiBold"
                                            VerticalAlignment="Center"/>
                                    <TextBlock Text=" — "
                                            Foreground="#999"
                                            VerticalAlignment="Center"/>
                                    <TextBlock Text="{Binding Descricao}"
                                            Foreground="#666"
                                            VerticalAlignment="Center"/>
                                </StackPanel>

                                <Button Grid.Column="1"
                                        Content="✖"
                                        Click="BtnExcluirCategoria_Click"
                                        Tag="{Binding Id}"
                                        Width="16"
                                        Height="16"
                                        Cursor="Hand"
                                        ToolTip="Excluir categoria">

                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background"
                                                    Value="Transparent"/>
                                            <Setter Property="Foreground"
                                                    Value="Black"/>
                                            <Setter Property="BorderThickness"
                                                    Value="0"/>

                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="Transparent">
                                                            <ContentPresenter
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>

                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver"
                                                        Value="True">
                                                    <Setter Property="Foreground"
                                                            Value="Red"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="2"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,15,0,0">
            <Button Content="Nova Categoria"
                    Click="BtnNovaCategoria_Click"
                    Width="120"
                    Margin="0,0,10,0"
                    Style="{StaticResource BotaoPadraoStyle}"/>
            <Button Content="Fechar"
                    Width="100"
                    Click="BtnFechar_Click"
                    Style="{StaticResource BotaoPadraoStyle}"/>
        </StackPanel>
    </Grid>
</Window>