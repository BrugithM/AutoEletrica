<UserControl x:Class="SgaAutoEletrica.UI.Views.Dashboard.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="Dashboard" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Grid.Column="0" Margin="5" Padding="15" 
                        Background="#3498DB" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="OS Abertas" Foreground="White" FontSize="14"/>
                        <TextBlock Text="{Binding Resumo.TotalOSAbertas}" Foreground="White" 
                                   FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="0" Grid.Column="1" Margin="5" Padding="15" 
                        Background="#E67E22" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="OS Em Andamento" Foreground="White" FontSize="14"/>
                        <TextBlock Text="{Binding Resumo.TotalOSEmAndamento}" Foreground="White" 
                                   FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="0" Grid.Column="2" Margin="5" Padding="15" 
                        Background="#27AE60" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Finalizadas Hoje" Foreground="White" FontSize="14"/>
                        <TextBlock Text="{Binding Resumo.TotalOSFinalizadasHoje}" Foreground="White" 
                                   FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Grid.Column="0" Margin="5" Padding="15" 
                        Background="#8E44AD" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Faturamento Hoje" Foreground="White" FontSize="14"/>
                        <TextBlock Text="{Binding Resumo.FaturamentoHoje, StringFormat=C2}" Foreground="White" 
                                   FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Grid.Column="1" Margin="5" Padding="15" 
                        Background="#C0392B" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="Estoque Baixo" Foreground="White" FontSize="14"/>
                        <TextBlock Text="{Binding Resumo.ProdutosEstoqueBaixo}" Foreground="White" 
                                   FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Button Content="Atualizar" Width="120" HorizontalAlignment="Left" 
                    Margin="5,20,0,0" Click="BtnAtualizar_Click"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>