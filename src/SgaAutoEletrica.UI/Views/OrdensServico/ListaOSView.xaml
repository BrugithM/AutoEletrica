<UserControl x:Class="SgaAutoEletrica.UI.Views.OrdensServico.ListaOSView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ordens de Serviço" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" 
                     Text="{Binding TermoBusca, UpdateSourceTrigger=PropertyChanged}"
                     Height="30" FontSize="14" Padding="5" Margin="0,0,10,0"
                     ToolTip="Busca por número, cliente ou placa"/>

            <ComboBox Grid.Column="1"
                      ItemsSource="{Binding StatusLista}"
                      SelectedItem="{Binding StatusFiltro}"
                      Height="30" Margin="0,0,10,0"/>

            <Button Grid.Column="2" Content="Buscar" Command="{Binding BuscarCommand}"
                    Width="80" Height="30" Margin="0,0,10,0"/>

            <Button Grid.Column="3" Content="Limpar" Command="{Binding LimparCommand}"
                    Width="80" Height="30" Margin="0,0,10,0"/>

            <Button Grid.Column="4" Content="+ Nova OS" Command="{Binding NovaOSCommand}"
                    Width="100" Height="30" Background="#27AE60" Foreground="White"/>
        </Grid>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Ordens}"
                  SelectedItem="{Binding OsSelecionada}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  MouseDoubleClick="DataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nº" Binding="{Binding Numero}" Width="50"/>
                <DataGridTextColumn Header="Cliente" Binding="{Binding NomeCliente}" Width="200"/>
                <DataGridTextColumn Header="Veículo" Binding="{Binding ModeloVeiculo}" Width="120"/>
                <DataGridTextColumn Header="Placa" Binding="{Binding PlacaVeiculo}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding ValorTotal, StringFormat=C2}" Width="90"/>
                <DataGridTextColumn Header="Abertura" Binding="{Binding DataAbertura, StringFormat=dd/MM/yyyy HH:mm}" Width="130"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Ver Detalhes" Command="{Binding VerDetalhesCommand}" 
                    Width="110" Height="30"/>
        </StackPanel>
    </Grid>
</UserControl>